import 'package:flutter/material.dart';

import 'os_page.dart';

class IOSPage extends OSPage {
  const IOSPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }

}